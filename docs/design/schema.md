-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.assessment_results (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  submitted_at timestamp with time zone DEFAULT now(),
  first_name text,
  last_name text,
  date_taken text,
  total_correct integer,
  total_questions integer,
  percentage integer,
  grade text,
  sec_a_score text,
  sec_a_pct integer,
  sec_b_score text,
  sec_b_pct integer,
  sec_c_score text,
  sec_c_pct integer,
  sec_d_score text,
  sec_d_pct integer,
  sec_e_score text,
  sec_e_pct integer,
  sec_f_score text,
  sec_f_pct integer,
  sec_g_score text,
  sec_g_pct integer,
  sec_h_score text,
  sec_h_pct integer,
  question_detail text,
  section_summary text,
  study_plan text,
  time_taken_seconds integer,
  time_taken_formatted text,
  CONSTRAINT assessment_results_pkey PRIMARY KEY (id)
);
CREATE TABLE public.python_assessment_results (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  submitted_at timestamp with time zone DEFAULT now(),
  first_name text,
  last_name text,
  date_taken text,
  total_correct integer,
  total_questions integer,
  percentage integer,
  grade text,
  time_taken_seconds integer,
  time_taken_formatted text,
  sec_a_score text,
  sec_a_pct integer,
  sec_b_score text,
  sec_b_pct integer,
  sec_c_score text,
  sec_c_pct integer,
  sec_d_score text,
  sec_d_pct integer,
  sec_e_score text,
  sec_e_pct integer,
  sec_f_score text,
  sec_f_pct integer,
  sec_g_score text,
  sec_g_pct integer,
  question_detail text,
  section_summary text,
  study_plan text,
  sec_h_score text,
  sec_h_pct integer,
  fitb_detail text,
  full_breakdown text,
  CONSTRAINT python_assessment_results_pkey PRIMARY KEY (id)
);
CREATE TABLE public.python_exercise_results (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  student_name text,
  submitted_at timestamp with time zone,
  total_score integer,
  s1_score integer,
  s2_score integer,
  s3_score integer,
  s4_score integer,
  s5_score integer,
  answers jsonb,
  attempts jsonb,
  w19_code text,
  w20_code text,
  w21_code text,
  w22_code text,
  w23_code text,
  w24_code text,
  w25_code text,
  CONSTRAINT python_exercise_results_pkey PRIMARY KEY (id)
);
CREATE TABLE public.caio_test_results (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  student_name text,
  submitted_at timestamp with time zone,
  mcq_score integer,
  blank_score integer,
  auto_score integer,
  mcq_details jsonb,
  q16_b1 text,
  q16_b2 text,
  q16_b3 text,
  q16_b4 text,
  q16_b5 text,
  q16_b6 text,
  q19_b1 text,
  q19_b2 text,
  q19_b3 text,
  q19_b4 text,
  q19_b5 text,
  q19_b6 text,
  q19_b7 text,
  q20_b1 text,
  q20_b2 text,
  q17_text text,
  q18_text text,
  q21_text text,
  CONSTRAINT caio_test_results_pkey PRIMARY KEY (id)
);
CREATE TABLE public.homework_submissions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  student_name text NOT NULL,
  class_number integer,
  homework_title text,
  score integer,
  total integer,
  pct integer,
  question_detail jsonb,
  started_at timestamp with time zone,
  submitted_at timestamp with time zone DEFAULT now(),
  access_code text,
  cohort text,
  CONSTRAINT homework_submissions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.access_codes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  code text NOT NULL UNIQUE,
  class_number integer NOT NULL,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  max_uses integer DEFAULT 999,
  uses_count integer DEFAULT 0,
  is_active boolean DEFAULT true,
  cohort text NOT NULL DEFAULT 'aijr'::text,
  CONSTRAINT access_codes_pkey PRIMARY KEY (id),
  CONSTRAINT access_codes_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.class_config (
  class_number integer NOT NULL,
  title text NOT NULL,
  subtitle text NOT NULL,
  description text,
  homework_title text,
  module_badge text,
  difficulty_badge text,
  exam_links text,
  msg_perfect text,
  msg_good text,
  msg_ok text,
  msg_retry text,
  CONSTRAINT class_config_pkey PRIMARY KEY (class_number)
);
CREATE TABLE public.questions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  class_number integer NOT NULL,
  question_number integer NOT NULL,
  section_number integer NOT NULL,
  section_title text NOT NULL,
  section_exam_note text,
  question_text text NOT NULL,
  code_snippet text,
  option_a text NOT NULL,
  option_b text NOT NULL,
  option_c text NOT NULL,
  option_d text NOT NULL,
  correct_answer integer NOT NULL CHECK (correct_answer >= 1 AND correct_answer <= 4),
  explanation text NOT NULL,
  hint_1 text,
  hint_2 text,
  is_exam_linked boolean DEFAULT false,
  exam_q_reference text,
  CONSTRAINT questions_pkey PRIMARY KEY (id),
  CONSTRAINT questions_class_number_fkey FOREIGN KEY (class_number) REFERENCES public.class_config(class_number)
);
CREATE TABLE public.project_progress_notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  student_name text NOT NULL,
  project_title text NOT NULL,
  session_date date,
  phase text,
  accomplishments text,
  confidence_rating smallint CHECK (confidence_rating >= 1 AND confidence_rating <= 5),
  ai_tools ARRAY DEFAULT '{}'::text[],
  tools_working text CHECK (tools_working = ANY (ARRAY['Yes'::text, 'No'::text, 'Partially'::text])),
  tools_issue text,
  seed_changed text CHECK (seed_changed = ANY (ARRAY['Yes'::text, 'No'::text])),
  seed_change_reason text,
  bias_identified text,
  responsible_use text,
  task_1 text,
  task_2 text,
  task_3 text,
  instructor_question text,
  slide_link text,
  CONSTRAINT project_progress_notes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.final_project_definitions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  student_name text NOT NULL,
  project_title text NOT NULL,
  submission_date date,
  project_type text,
  one_sentence text,
  ai_tools ARRAY DEFAULT '{}'::text[],
  audience text,
  problem_solved text,
  how_used text,
  core_feature text,
  feature_1 text,
  feature_2 text,
  feature_3 text,
  success_looks_like text,
  limitations text,
  responsible_checks ARRAY DEFAULT '{}'::text[],
  responsible_choice text,
  elevator_pitch text,
  most_excited text,
  CONSTRAINT final_project_definitions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.caio_final_exam_results (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  student_name text NOT NULL,
  class_number integer DEFAULT 16,
  access_code text,
  submitted_at timestamp with time zone DEFAULT now(),
  mcq_score integer,
  blank_score integer,
  auto_score integer,
  mcq_details jsonb,
  q16_b1 text,
  q16_b2 text,
  q16_b3 text,
  q16_b4 text,
  q16_b5 text,
  q16_b6 text,
  q19_b1 text,
  q19_b2 text,
  q19_b3 text,
  q19_b4 text,
  q19_b5 text,
  q19_b6 text,
  q20_b1 text,
  q20_b2 text,
  q20_b3 text,
  q20_b4 text,
  q20_b5 text,
  q17_text text,
  q18_text text,
  q21_text text,
  CONSTRAINT caio_final_exam_results_pkey PRIMARY KEY (id)
);
CREATE TABLE public.cohorts (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  name text NOT NULL UNIQUE,
  description text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  created_by uuid,
  CONSTRAINT cohorts_pkey PRIMARY KEY (id),
  CONSTRAINT cohorts_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);